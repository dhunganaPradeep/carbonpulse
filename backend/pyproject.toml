[project]
name = "carbonpulse-backend"
version = "0.1.0"
description = "CarbonPulse backend (FastAPI + SQLAlchemy 2.0 async)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
